<?xml version="1.0" encoding="UTF-8"?>
<beans:beans
	xmlns:security="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd 
  http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd">

	<!-- import security configuration, custom pre-auth filter implementations -->
	<beans:import resource="classpath:ltpa-security-context.xml" />
		
	<!-- Apply specific security against a endpoint -->
	<security:http auto-config="true" use-expressions="true" entry-point-ref="preAuthenticatedProcessingFilterEntryPoint" create-session="never">
		<security:intercept-url pattern="/**" access="hasRole('ROLE_APIUSER')" />
		<security:custom-filter position="PRE_AUTH_FILTER" ref="preAuthFilter" />
		<security:access-denied-handler/>
		
		<!-- Disable CSRF -->
		<security:csrf disabled="true" />
	</security:http>
	
	<!-- Enable Global security 
	<security:global-method-security secured-annotations="enabled" pre-post-annotations="enabled">
    </security:global-method-security>
-->
	<!--  -->
	<beans:bean id="preAuthenticatedProcessingFilterEntryPoint" class="org.springframework.security.web.authentication.Http403ForbiddenEntryPoint" />

</beans:beans>